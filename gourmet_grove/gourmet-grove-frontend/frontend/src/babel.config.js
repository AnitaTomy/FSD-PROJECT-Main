module.exports = {
    presets: [
      ["@babel/preset-react", { throwIfNamespace: false }],
      // Add other presets if needed
    ],
    // Add other Babel configuration options if needed
  };
  